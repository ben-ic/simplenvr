# SimpleNVR — Claude Code context

## Design direction

**See `.impeccable.md` for the authoritative Design Context** — brand personality, aesthetic direction, palette, accessibility requirements, and non-negotiable principles. Read that file before designing any UI, writing any user-facing copy, or making any visual change. When a proposal conflicts with anything in `.impeccable.md`, the proposal changes — not the document.

## Product direction

**See `docs/product.md`** for the product north star, target audience, explicit non-goals, and the "can a non-technical user do this?" test every feature must pass.

## Architecture

**See `docs/architecture.md`** for the technical architecture: Tauri Rust shell, Python sidecar, go2rtc relay, ffmpeg recording pipeline, lifecycle invariants, and the subsystem breakdown.
