fastapi>=0.115.0
uvicorn[standard]>=0.30.0
onvif-zeep-async>=4.0.0,<5.0.0
aiosqlite>=0.20.0
platformdirs>=4.0.0
# Classification subsystem — MOG2 background subtraction, morphology,
# NCC template matching, JPEG encode for event thumbnails. See
# docs/cv2-selfbuild.md for the LGPL-clean build rationale: stock
# opencv-python-headless on PyPI bundles an FFmpeg linked against
# GPL libx264/libx265, which would infect our closed-source bundle.
# For platforms with a committed wheel in vendor/cv2-wheels/, pip
# installs that (no FFmpeg, no GPL deps). For platforms without one,
# pip falls through to stock PyPI and scripts/bundle_python.{sh,ps1}
# enforces LGPL-clean at bundle time (force-reinstall from wheel +
# post-bundle scan that fails if GPL deps leak through).
# On macOS arm64, install the self-built wheel matching your Python version from vendor/cv2-wheels/ after building it with scripts/build_cv2_wheel.sh
# Example: pip install vendor/cv2-wheels/opencv_python_headless-<version>-cp<pyver>-cp<pyver>-macosx_<ver>_arm64.whl
onnxruntime>=1.17.0
psutil>=5.9.0
opencv-python-headless>=4.9.0
# scipy: ByteTrack's Kalman filter uses scipy.linalg.cho_factor/cho_solve
# for numerical stability vs np.linalg.inv, and scipy.optimize.linear_sum_assignment
# is the Hungarian solver for the tracker's two-pass association. Also used
# by the detector's connected-components step (scipy.ndimage.label) to extract
# motion proposals from the foreground mask.
scipy>=1.13.0

# Ecosystem publication — HomeKit bridge (Phase 1A).
# Exact pin matches Home Assistant's pin: HA co-maintains HAP-python, so
# their bump cadence is the canary for iOS-compatibility regressions.
# Loosening to a range would put our pair flow at the mercy of upstream
# breaking changes that HA catches first.
HAP-python[qrcode]==5.0.0
# homekit-audio-proxy — rewrites ffmpeg's hardcoded 48kHz Opus RTP clock
# to HomeKit's negotiated 16/24kHz. Required by M6 (audio) but deferred
# until then because versions >=1.1.0 require Python >=3.13 and this
# project is on 3.11. Version 1.0.0 ships on 3.11 but PyPI classifies it
# "Development Status :: 2 - Pre-Alpha" with no cryptography dep. M6
# will resolve by bumping Python, swapping libraries, or handling the
# clock-rate rewrite inline. M1 ships video-only so this isn't blocking.
# homekit-audio-proxy==1.2.1

